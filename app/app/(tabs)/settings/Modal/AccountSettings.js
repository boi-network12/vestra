import { View, Text } from 'react-native'

export default function AccountSettings({ colors }) {


  return (
    <View>
      <Text style={{ color: colors.text }}>Account Settings</Text>
      
    </View>
  )
}